# Syntecxhub_Maze-Solver-using-A-Search
Represent a maze/grid (start, goal, walls) and model it as nodes. Implement A* search with a heuristic (Manhattan or Euclidean). Return shortest path and handle unreachable cases. Optionally visualize the search &amp; final path (console or simple plot)
